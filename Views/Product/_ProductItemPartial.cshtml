@using Domain.Entities
@* @using Domain.Entities
@using Domain.Models
@model ProductListModel<Dish>
@{
    var request = ViewContext.HttpContext.Request;
    var returnUrl = request.Path + request.QueryString.ToUriComponent();
    var selectedCategory = ViewBag.SelectedCategory as string;
    var categories = ViewBag.Categories as List<Category> ?? new List<Category>();

}
<div class="col-md-9">

    <h3>@(selectedCategory != null
                ? $"Категория: {categories.FirstOrDefault(c => c.NormalizedName == selectedCategory)?.Name}"
                : "Все блюда")</h3>

    <div class="row">
        @foreach (var dish in Model.Items)
        {
            <div class="col-md-6 col-lg-4 mb-4">
                <div class="card h-100 d-flex flex-column">

                    @if (!string.IsNullOrEmpty(dish.Image))
                    {
                        <img src="@dish.Image" class="card-img-top" alt="@dish.Name" style="height: 200px; object-fit: cover;">
                    }
                    else
                    {
                        <div style="height: 200px; background-color: #e9ecef; display: flex; 
                                                                    align-items: center; justify-content: center;">
                            <span class="text-muted">Нет изображения</span>
                        </div>
                    }

                    <div class="card-body d-flex flex-column">
                        <h5 class="card-title">@dish.Name</h5>
                        <p class="card-text">@dish.Description</p>

                        <div class="d-flex justify-content-between align-items-center mt-auto mb-3">
                            <span>
                                @if (dish.Weight.HasValue)
                                {
                                    <span>Вес: @dish.Weight г.</span>
                                }
                            </span>
                            <strong>@dish.Price.ToString("C")</strong>
                        </div>

                        <a href="@Url.Action("Add", "Cart", new { id = dish.Id, returnUrl = returnUrl })"
                            class="btn btn-primary w-100">
                            В корзину
                        </a>
                    </div>
                </div>
            </div>
        }
    </div> *@


@* @model Dish

<div class="col-md-6 col-lg-4 mb-4">
    <div class="card h-100 d-flex flex-column">

        @if (!string.IsNullOrEmpty(Model.Image))
        {
            <img src="@Model.Image" class="card-img-top" alt="@Model.Name"
                 style="height: 200px; object-fit: cover;">
        }
        else
        {
            <div style="height: 200px; background-color: #e9ecef; display: flex;
                        align-items: center; justify-content: center;">
                <span class="text-muted">Нет изображения</span>
            </div>
        }

        <div class="card-body d-flex flex-column">
            <h5 class="card-title">@Model.Name</h5>
            <p class="card-text">@Model.Description</p>

            <div class="d-flex justify-content-between align-items-center mt-auto mb-3">
                <span>
                    @if (Model.Weight.HasValue)
                    {
                        <span>Вес: @Model.Weight г.</span>
                    }
                </span>

                <strong>@Model.Price.ToString("C")</strong>
            </div>

            <a href="@Url.Action("Add", "Cart", new { id = Model.Id })"
               class="btn btn-primary w-100">
                В корзину
            </a>
        </div>
    </div>
</div> *@

@model Dish

<div class="card h-100 d-flex flex-column">

    @if (!string.IsNullOrEmpty(Model.Image))
    {
        <img src="@Model.Image"
             class="card-img-top"
             alt="@Model.Name"
             style="height: 200px; object-fit: cover;">
    }
    else
    {
        <div style="height: 200px; background-color: #e9ecef; 
                    display: flex; align-items: center; justify-content: center;">
            <span class="text-muted">Нет изображения</span>
        </div>
    }

    <div class="card-body d-flex flex-column">
        <h5 class="card-title">@Model.Name</h5>
        <p class="card-text">@Model.Description</p>

        <div class="d-flex justify-content-between align-items-center mt-auto mb-3">
            <span>
                @if (Model.Weight.HasValue)
                {
                    <span>Вес: @Model.Weight г.</span>
                }
            </span>

            <strong>@Model.Price.ToString("C")</strong>
        </div>

        <a href="@Url.Action("Add", "Cart", new { id = Model.Id })"
           class="btn btn-primary w-100">
            В корзину
        </a>
    </div>

</div>
